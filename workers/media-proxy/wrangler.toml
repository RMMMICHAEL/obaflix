name = "obaflix-media"
main = "src/worker.ts"
compatibility_date = "2026-08-30"

# Sem KV, sem D1, sem Durable Objects: o Worker não guarda estado. Tudo que ele
# precisa saber vem assinado no pedido.

[vars]
# Origem única autorizada no CORS. Não use "*": seria proxy aberto para
# qualquer site que descobrisse uma URL assinada.
APP_ORIGIN = "https://obaflix.vercel.app"

# CDN_ALLOWLIST, CANAIS_PLAYER_ALLOWLIST e CANAIS_MEDIA_BASE vivem somente na
# configuração remota do Worker. Publique sempre com `wrangler deploy --keep-vars`.

# Sufixos permitidos para a PÁGINA que arma o grant de canal. Lista própria, e
# não a de CDN: são papéis diferentes, com hosts diferentes. Juntá-las daria a um
# host de CDN comprometido o direito de ser buscado como página de arm.
# Vazia recusa o arm — nunca o libera.
# Base pública deste Worker. O mesmo valor de CANAIS_MEDIA_BASE no backend.
# ASSINATURA_SECRET NÃO vai aqui. É o CANAIS_MEDIA_SIGNING_SECRET do backend —
# um segredo próprio, criado só para isto, e NÃO o NEXTAUTH_SECRET: este Worker
# roda em infra de terceiro, e um vazamento aqui não pode alcançar a assinatura
# de sessão de autenticação do produto.
#   npx wrangler secret put ASSINATURA_SECRET
#   npx wrangler secret put UPSTASH_REDIS_REST_URL
#   npx wrangler secret put UPSTASH_REDIS_REST_TOKEN
