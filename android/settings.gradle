pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Obaflix"

// Extracao de midia: provedores, manifesto HLS, headers e diagnostico de rede.
// Compartilhado entre o app movel e o app de TV para que os dois nao divirjam —
// e a divergencia silenciosa entre ambientes ja custou caro antes.
include(":core-extractor")

include(":app")   // movel  — WebView + ponte
include(":tv")    // Android TV / TV Box — Compose for TV
